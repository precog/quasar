#!/usr/bin/env bash

if ! type "aws" > /dev/null; then
  pip install awscli --user
fi

AWS=$(command -v aws)
TARGETS=".targets"
HOARDER=".hoarder-cache"
TRAVISIP=$(curl -s checkip.dyndns.org | sed -e 's/.*Current IP Address: //' -e 's/<.*$//')

echo "Trivs public IP is: $TRAVISIP"
./scripts/credentials

if [[ "$1" = "upload" ]]; then
  T=$(aws s3 --only-show-errors cp $TARGETS s3://quasar-build-cache/$TRAVIS_BUILD_NUMBER/$TARGETS --recursive)
  echo $T
  H=$(aws s3 --only-show-errors cp $HOARDER s3://quasar-build-cache/$TRAVIS_BUILD_NUMBER/$HOARDER --recursive)
  echo $H
elif [[ "$1" = "download" ]]; then
  T=$(aws s3 --only-show-errors cp s3://quasar-build-cache/$TRAVIS_BUILD_NUMBER/$TARGETS $TARGETS --recursive)
  echo $T
  H=$(aws s3 --only-show-errors cp s3://quasar-build-cache/$TRAVIS_BUILD_NUMBER/$HOARDER $HOARDER --recursive)
  echo $H
elif [[ "$1" = "cleanup" ]]; then
  T=$(aws s3 --only-show-errors rm s3://quasar-build-cache/$TRAVIS_BUILD_NUMBER --recursive)
  echo $T
else
  echo "Usage: ./scripts/s3Cache upload | download | cleanup"
fi
